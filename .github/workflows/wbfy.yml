name: Willboosterify
on:
  schedule:
    - cron: 0 5 * * 0
  workflow_dispatch:
jobs:
  wbfy:
    uses: WillBooster/reusable-workflows/.github/workflows/wbfy.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GH_BOT_PAT }}

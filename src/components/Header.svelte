<div>PlantUML Visualizer</div>

<style lang="scss">
  div {
    background-color: var(--color-surface-primary);
    border-bottom: 1px solid var(--color-border-secondary);
    font-weight: bold;
    padding: var(--space-window) var(--space-window) var(--space-md);
    text-align: center;
    user-select: none;
  }
</style>
